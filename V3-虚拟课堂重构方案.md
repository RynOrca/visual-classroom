# V3 · 虚拟课堂重构方案

> 状态：📋 方案已确认，进入实施准备  
> 创建：2026-07-13  
> 定位：以 Open Notebook 为基座，重构为真正的“虚拟课堂”

---

## 1. 背景与目标

### 1.1 背景

当前项目（V2.x）已经具备三栏课堂、PDF/PPT 解析、OCR、AI 讲义、问答、知识地图、复习等能力，但存在以下问题：

- 前端仍是“毛坯房”：大量 inline style，组件化程度低，视觉和交互不够产品化。
- OCR 管道是同步阻塞的，且 OCR 引擎写死为 RapidOCR。
- 数据模型停留在“文档 → 页面”，没有“章节 / 知识点”层，导致知识地图、问答整理、错题本无法形成闭环。
- 模块边界不清晰，后端路由过厚，不利于继续扩展。
- 没有利用现成的开源 NotebookLM 类基座，重复造轮子成本高。

### 1.2 目标

构建一个单机本地版的“虚拟课堂”：

1. 上传课件（PDF / PPT / MD / TXT）后，AI 按章节/页面进行教学。
2. 默认两栏：左课件，右 AI 讲义/笔记；点击“问答”再弹出第三栏对话。
3. 支持划词问答、费曼翻转、类比解释。
4. 问答由子 Agent 自动按知识点整理，形成“知识笔记本”。
5. 支持错题本、刷题、薄弱点分析。
6. 复习时先俯瞰章节演进关系，再逐层下钻到知识点和原课件位置。
7. 以 Open Notebook 为基座，引入上述特色能力，而不是从零重写。

### 1.3 非目标（V3 暂不做）

- 多用户 / 账号 / 云同步
- 移动端优先
- 大规模团队协作
- 在线部署（可后置）

---

## 2. 技术路线

### 2.1 基座策略：Fork Open Notebook 全量二次开发（已确定）

- 仓库：`https://github.com/lfnovo/open-notebook`
- 技术栈（已通过检索确认）：
  - 前端：Next.js 15 + React 19 + TypeScript + Tailwind CSS + shadcn/ui + Zustand
  - 后端：Python FastAPI + LangGraph / LangChain
  - 数据库：SurrealDB
  - 任务：后台 worker 处理解析、嵌入、转换
- 决策：采用 **Fork 全量改源码**，以获得最完整、最顺滑的体验。
- 我们会在 fork 中新增一个独立的 `virtual_classroom` 模块/目录，集中放置“虚拟课堂”特色代码，尽量少改上游核心文件，方便后续跟踪上游。

我们要在 Open Notebook 之上增加：

- 课件章节结构抽取
- 两栏/三栏课堂交互
- 知识地图（章节演进 + 俯瞰 + 下钻）
- 对话整理 Agent
- 错题本 / 刷题 / 复习路线
- UnlimitedOCR 本地 OCR 接入

### 2.2 为什么选 Fork，而不是 API / 内嵌

| 方案 | 体验 | 结论 |
|---|---|---|
| 本地服务 + API | 需要自建前端、多跑一个服务，课堂 UI 与 Notebook 基础功能割裂 | 不选 |
| Python 模块内嵌 | 仍需 SurrealDB + worker，且要自建前端，耦合深 | 不选 |
| Fork 全量改源码 | 单应用、单 UI，直接复用成熟界面和 RAG，体验最顺滑 | ✅ 选择 |

实施前仍需在 Phase 0 确认：

- 源码能否本地启动（含 SurrealDB / worker）
- 数据模型和路由是否容易扩展
- 能否安全加入自定义页面、表和 API
- OCR 是否能接入其 source 处理链路

### 2.3 本地 Python 工具链

- **OCR**：优先使用 `UnlimitedOCR`（本地 Python 库），保留 RapidOCR 作为 fallback。
- **PDF 文本层检测**：PyMuPDF。
- **PPT 转 PDF**：LibreOffice（已有能力可复用）。
- **接入方式**：在 Open Notebook 的 source 处理链路（content-core / source_graph）中，增加“文本层检测 → OCR Provider”步骤，OCR 结果按页/片段进入现有 Source 文本，再走嵌入和 RAG。
- 所有 OCR / 解析均为本地执行，不上传外部服务。

---

## 3. 总体架构

### 3.1 逻辑分层

```
┌─────────────────────────────────────────────┐
│         Fork 后的 Open Notebook UI          │
│  两栏课堂 / 弹出式问答 / 知识地图 / 复习      │
└─────────────────────┬───────────────────────┘
                      │ HTTP / SSE / WebSocket
┌─────────────────────▼───────────────────────┐
│            Open Notebook 核心               │
│  文档管理 / 解析 / 向量检索 / 聊天 / 来源引用 │
└─────────────────────┬───────────────────────┘
                      │ 同进程内扩展模块
┌─────────────────────▼───────────────────────┐
│            虚拟课堂特色模块                    │
│  章节抽取 / 知识地图 / 对话整理 / 错题本       │
└─────────────────────┬───────────────────────┘
                      │
┌─────────────────────▼───────────────────────┐
│              本地基础设施                     │
│  SurrealDB / 本地文件 / Python OCR          │
└─────────────────────────────────────────────┘
```

### 3.2 模块划分（按业务域）

在 Fork 后的仓库中，我们的代码尽量集中在独立目录：

```
open_notebook/virtual_classroom/
├── ingestion/       # 上传、解析、OCR、章节抽取
├── teaching/        # 讲义、问答、类比、费曼、对话整理
├── knowledge/       # 知识地图、知识点、语义检索
├── practice/        # 出题、刷题、错题本、薄弱点
├── review/          # 总复习、歌词回顾、播客、复习路线
└── settings/        # LLM Provider、用户画像、OCR 配置

frontend/app/(virtual-classroom)/
├── classroom/       # 两栏/三栏课堂页
├── knowledge-map/   # 知识地图页
├── practice/        # 刷题/错题本页
└── review/          # 复习页
```

每个模块尽量独立：自带路由、服务、数据模型、Prompt、前端页面/组件。

### 3.3 数据模型（核心扩展）

在 Open Notebook 自带数据模型之外，新增：

```
subject            # 科目
  └── document     # 课件（可来自 Open Notebook 的 source/notebook）
        └── chapter        # 章节
              └── page     # 页面/片段
                    └── knowledge_point  # 知识点

lecture            # AI 讲义（按 page 或 chapter 生成）
chat_message       # 问答记录（挂到 document/page/knowledge_point）
knowledge_tag      # 问答自动提取的知识点标签
knowledge_map      # 章节演进知识地图（JSON）
mistake_book       # 错题本
quiz_session       # 刷题记录
conversation_note  # 子 Agent 整理的“知识笔记本”卡片
```

以上扩展实体使用 SurrealDB 实现，并尽量复用 Open Notebook 的 domain / repository 模式。

### 3.4 文档导入管道

```
上传文件
  → 保存到本地
  → 类型识别（PDF / PPT / MD / TXT）
  → PPT 转 PDF（如需）
  → 文本层检测（PyMuPDF）
  → 若扫描版 → 后台 OCR（UnlimitedOCR / RapidOCR fallback）
  → 按页规范化文本
  → 章节结构抽取（目录 / 标题 / LLM 辅助）
  → 状态通知前端（none / processing / done / failed）
```

要求：

- 上传接口立即返回，OCR 和解析异步执行。
- 前端可轮询或通过 SSE 接收进度。
- OCR Provider 可插拔：`UnlimitedOCRProvider` / `RapidOCRProvider`。

---

## 4. 前端设计

### 4.1 布局策略

保留三栏核心，但默认更灵活：

```
默认两栏：
┌──────────────────┬─────────────────────────┐
│  左：课件阅读     │  右：AI 讲义 / 笔记       │
│  PDF / PPT       │  章节导航 + 知识点卡片    │
└──────────────────┴─────────────────────────┘

点击“问答”后：
┌──────────────────┬─────────────────────────┬──────────────┐
│  左：课件阅读     │  右：AI 讲义 / 笔记       │  第三栏：问答  │
└──────────────────┴─────────────────────────┴──────────────┘
```

- 第三栏可以是抽屉、浮层或可拖拽分栏，不强制常驻。
- 移动端/窄屏时自动折叠为单栏 + 底部按钮。

### 4.2 主要页面

| 页面 | 内容 |
|---|---|
| 首页 | 科目列表、最近学习、继续学习 |
| 科目工作台 | 知识地图 / 课件列表 / 总复习 / 错题本 / 刷题 |
| 课堂 | 两栏/三栏学习空间 |
| 复习 | 章节俯瞰 → 知识点下钻 → 原课件跳转 |

### 4.3 视觉

- 由 Gemini 重新设计前端，输出设计稿/设计令牌。
- 使用 Tailwind + shadcn/ui 或 Open Notebook 自带 UI 体系。
- 保持三套主题能力（浅色 / 暗色 / 纸质）或按新设计统一。

---

## 5. 特色功能规划

### 5.1 章节演进知识地图（核心差异）

- 输入：科目下所有课件的章节结构 + 讲义 + 问答 + 错题。
- 输出：`stages[]`，每个 stage 对应一个章节/课件，包含：
  - `label`：章节标题
  - `summary`：这一章解决什么问题
  - `bridgeToNext`：为什么引出下一章
  - `concepts[]`：核心知识点
- 支持三级缩放：
  - L1 俯瞰：章节演进故事线
  - L2 框架：章节内知识点
  - L3 痛点：错题/高频问题热力标记，点击回到原页面/对话

### 5.2 对话整理 Agent

- 一次问答可能包含多种问题类型：定义、推导、例子、易错点、应用。
- 问答结束后，子 Agent 自动：
  - 提取知识点标签
  - 判断问题类型
  - 生成“知识卡片”进入笔记本
- 复习时可按“知识点 / 问题类型 / 时间”回顾。

### 5.3 错题本与刷题

- 基于已有 V2.2 方案升级：
  - 多题型：单选 / 多选 / 填空 / 简答
  - 批量出题：按章节 / 知识点
  - 自动收集错题
  - 连续答对出狱
  - 薄弱点统计 + 知识地图热力联动

### 5.4 复习路线

- 先“俯瞰”：AI 讲清章节为什么这样排。
- 再“下钻”：按知识点逐层复习。
- 支持“歌词式回顾”、播客、思维导图等已有能力，但统一挂到知识地图上。

---

## 6. 开发阶段

### Phase 0：Fork 前验证（1-2 天）

- [ ] clone Open Notebook，确认许可证、本地启动方式（源码 + SurrealDB + worker）。
- [ ] 跑通一个最小示例：上传 PDF → 问答 → 引用来源。
- [ ] 验证本地 UnlimitedOCR 能否接入其 source 处理链路。
- [ ] 确认自定义页面、表、API 的扩展点。

### Phase 1：Fork 落地 + 数据模型（2-3 天）

- [ ] Fork Open Notebook，建立 `virtual_classroom` 模块目录。
- [ ] 设计并落地扩展表：chapter、knowledge_point、mistake_book 等。
- [ ] 定义模块路由挂载方式和前端页面路由。
- [ ] 将当前 V2 的 LLM Provider 配置/经验适配到 Open Notebook。

### Phase 2：文档导入管道（2-3 天）

- [ ] 异步上传与解析任务。
- [ ] 接入 UnlimitedOCR + RapidOCR fallback。
- [ ] 页面文本规范化存储。
- [ ] 章节结构抽取。
- [ ] 前端显示解析进度。

### Phase 3：课堂 UI（3-5 天）

- [ ] 两栏课堂布局。
- [ ] 弹出式第三栏问答。
- [ ] 划词提问、引用来源、流式输出。
- [ ] 章节导航与页面跳转。
- [ ] 应用 Gemini 新设计稿。

### Phase 4：教学与问答闭环（3-5 天）

- [ ] AI 讲义生成（按页/按章）。
- [ ] 问答保存 + 知识点标签提取。
- [ ] 对话整理 Agent / 知识卡片。
- [ ] 费曼翻转、类比引擎迁移。

### Phase 5：知识地图与复习（3-5 天）

- [ ] 章节演进知识地图。
- [ ] 知识地图 ↔ 课件/问答/错题跳转联动。
- [ ] 总复习 / 歌词回顾 / 播客 / 思维导图迁移。
- [ ] 错题本与刷题。

### Phase 6：打磨与验收（2-3 天）

- [ ] 全流程走通：上传 → 教学 → 问答 → 整理 → 复习 → 错题。
- [ ] 性能与体验优化。
- [ ] 清理 V2 遗留无用代码。
- [ ] 更新 CHANGELOG 和开发文档。

---

## 7. 与现有代码的关系

- 现有 V2 代码**不设为不可删除**，但不再作为主代码库。
- 由于已确定 Fork Open Notebook，现有 `frontend/` 和 `server/` 主要作为：
  - Prompt 模板迁移来源
  - 知识地图 / 复习 / 错题本等功能参考
  - 部分可直接复用的服务（如 PPT 转 PDF、OCR 脚本、LLM Provider 配置）
- 新代码以 Fork 后的 Open Notebook 仓库为主，课堂特色集中在 `virtual_classroom` 模块。

### 建议保留

- `design-refs/`
- `知识地图模块.md`
- `.omo/plans/v2.2-出题模式-错题本.md`（升级为 V3 错题本需求）
- OCR 脚本与 PPT 转换脚本

### 建议删除/重写

- 当前前端大量 inline style 页面
- 当前后端过厚的 routes
- 与 Open Notebook 重复的文档管理、聊天基础能力

---

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| Fork 后难以跟上上游更新 | 核心代码少改，课堂功能集中在 `virtual_classroom`；定期 merge upstream |
| Open Notebook 本地环境较重（SurrealDB + worker） | Phase 0 验证源码启动；必要时提供一键启动脚本 / 打包方案 |
| SurrealDB 数据模型扩展有学习成本 | 先按 Open Notebook 的 domain / repository 模式新增表，不绕开其规范 |
| UnlimitedOCR 识别质量/速度不稳定 | 保留 RapidOCR fallback，可配置切换 |
| 异步 OCR / 解析增加复杂度 | 沿用 Open Notebook 自带 worker 机制，不做分布式 |
| 重构范围过大导致长期不可用 | 采用“Fork 先跑通 + 特色功能逐个接入”的方式，每个阶段可交付 |

---

## 9. 验收标准（V3 Done）

- [ ] 基于 Fork 后的 Open Notebook 的本地版本可一键启动。
- [ ] 上传 PDF/PPT 后自动解析文本，扫描版自动 OCR。
- [ ] 默认两栏课堂，点击“问答”可弹出第三栏。
- [ ] AI 能按章节/页面教学，支持划词提问。
- [ ] 问答自动整理为知识卡片/知识点。
- [ ] 知识地图展示章节演进关系，可点击跳回原课件和对话。
- [ ] 错题本与刷题可用，并联动知识地图热力。
- [ ] 复习路线支持“俯瞰 → 下钻”。
- [ ] 单机本地运行，无需外部服务（除用户自配 LLM API）。
